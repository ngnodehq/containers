---
name: Release on Merge

concurrency:
  group: container-release
  cancel-in-progress: false

on:
  push:
    branches: [main]
    paths:
      - apps/**
      - .github/scripts/templates/**
      - "!apps/**/metadata.json"
      - "!apps/**/metadata.yaml"
      - "!apps/**/README.md"

jobs:
  simple-checks:
    uses: ngnodehq/containers/.github/workflows/simple-checks.yaml@308dcd7b2c6263fd13925ca035ac560f7350b272 # main

  get-changed-images:
    needs: [simple-checks]
    uses: ngnodehq/containers/.github/workflows/get-changed-images.yaml@308dcd7b2c6263fd13925ca035ac560f7350b272 # main

  build-images:
    needs: [simple-checks, get-changed-images]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: ngnodehq/containers/.github/workflows/build-images.yaml@308dcd7b2c6263fd13925ca035ac560f7350b272 # main
    secrets: inherit
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: true
      sendNotifications: true

  render-readme:
    name: Render Readme
    needs: build-images
    if: ${{ always() && needs.build-images.result != 'failure' }}
    uses: ./.github/workflows/render-readme.yaml
    secrets: inherit
